<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <head>
    </head>
    <body>
        <p><code>update-testing-rollback</code> tests rollback support by deploying all pending <MadCap:variable name="General.changeset" />s to the database, executes a rollback sequentially for the equal number of <MadCap:variable name="General.changeset" />s that were deployed, and then runs the update again deploying all <MadCap:variable name="General.changeset" />s to the database.</p>
        <h2>Uses</h2>
        <p><code>update-testing-rollback</code> is typically used when you want to test rollback functionality when deploying <MadCap:variable name="General.changeset" />s in your <MadCap:variable name="General.changelog" /> sequentially. Run <code>update-testing-rollback</code> only when all pending <MadCap:variable name="General.changelog" />s have been verified as ready to be deployed as you cannot specify <MadCap:variable name="General.changeset" />s to exclude.</p>
        <p><code>update-testing-rollback</code> utilizes a multi-step operation and runs in sequential order:</p>
        <ol>
            <li>update changeset1; update changeset2; update changeset3</li>
            <li>rollback changeset3; rollback changeset2; rollback changeset1</li>
            <li>update changeset1; update changeset2 update changeset3</li>
        </ol>
        <p class="note" MadCap:autonum="&lt;b&gt;Note: &lt;/b&gt;">If you use the earlier versions of <MadCap:variable name="General.Liquibase" />, the order of operations may differ.</p>
        <p>If any of the <MadCap:variable name="General.changeset" />s fail rollback, <MadCap:variable name="General.changeset" />s that come after the failed <MadCap:variable name="General.changeset" />s will not be tested or updated. Any <MadCap:variable name="General.changeset" />s that come before the failed <MadCap:variable name="General.changeset" />s will be deployed to the database.</p>
    </body>
</html>