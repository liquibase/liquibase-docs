<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd" MadCap:searchable="False">
    <head>
    </head>
    <body>
        <p>Name of the default schema to use for the database connection. If <code class="language-text">defaultSchemaName</code> is set, then  objects do not have to be fully qualified. This means you can refer to just <code class="language-text">mytable</code> instead of <code>myschema.mytable</code>.</p>
        <p class="note" MadCap:autonum="&lt;b&gt;Note: &lt;/b&gt;">In the properties file and <code class="language-text">JAVA_OPTS</code> only: in 4.18.0 and earlier, specify this parameter using the syntax <code class="language-text">defaultSchemaName</code>. In 4.19.0 and later, use the syntax <code class="language-text">liquibase.command.defaultSchemaName</code>.</p>
        <p class="note" MadCap:autonum="&lt;b&gt;Note: &lt;/b&gt;">In <MadCap:variable name="General.Liquibase" /> 4.22.0 and earlier, <MadCap:variable name="General.Liquibase" /> halts if you specify a mixed-case value of <code class="language-text">defaultSchemaName</code> (like <code class="language-text">myDbSchema</code>), so you must specify a lowercase value (like <code class="language-text">mydbschema</code>). In 4.23.0 and later, <MadCap:variable name="General.Liquibase" /> accepts any casing as valid. (By default, <MadCap:variable name="General.Liquibase" /> does not treat schema name as case-sensitive. However, if you set <code>--preserve-schema-case</code> as <code class="language-text">true</code>, the casing you specify must also match your database casing.)</p>
    </body>
</html>