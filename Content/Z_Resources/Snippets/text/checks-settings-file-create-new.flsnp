<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <head>
    </head>
    <body>
        <p>Run any policy check command:</p><pre><code class="language-shell">liquibase checks show</code></pre>
        <p>Enter <code>1</code> to use the default checks settings file (<code>liquibase.checks-settings.conf</code>). When successful, Liquibase displays a list of checks enabled by default plus the following message:</p><pre><code>liquibase.checks-settings.conf successfully created!</code></pre>
    </body>
</html>