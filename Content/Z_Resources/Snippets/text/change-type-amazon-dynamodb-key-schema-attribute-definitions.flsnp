<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <body>
        <p>You must specify all attributes you use in <code class="language-text">keySchema</code> within individual <code class="language-text">attributeDefinition</code> objects in the <code class="language-text">attributeDefinitions</code> array. For example, if your key schema contains the hash attribute <code class="language-text">Artist</code>, you must also specify <code class="language-text">Artist</code> and its data type in an <code class="language-text">attributeDefinition</code>. However, <code class="language-text">attributeDefinitions</code> can contain additional non-key attributes that aren't in <code class="language-text">keySchema</code>.</p>
    </body>
</html>