<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <body>
        <p>You must specify all attributes you use in <code>keySchema</code> within individual <code>attributeDefinition</code> objects in the <code>attributeDefinitions</code> array. For example, if your key schema contains the hash attribute <code>Artist</code>, you must also specify <code>Artist</code> and its data type in an <code>attributeDefinition</code>. However, <code>attributeDefinitions</code> can contain additional non-key attributes that aren't in <code>keySchema</code>.</p>
    </body>
</html>