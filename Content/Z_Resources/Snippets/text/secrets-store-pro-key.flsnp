<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <head>
    </head>
    <body>
        <p>The vault extension requires a <MadCap:variable name="General.LBPro" /> license key in your <code><MadCap:variable name="General.liquiPropFile" /></code> file. However, you may want to store your license key in your vault.</p>
        <p>To do this, authenticate with your vault by setting the <code>VAULT_ADDR</code> and <code MadCap:conditions="secrets.cyberark">VAULT_CERTFILE</code><code MadCap:conditions="secrets.hashicorp">VAULT_TOKEN</code>&#160;<MadCap:variable name="General.Param/Attribute" />s as environment variables or <code>-D</code> Java system properties instead of using them in <code><MadCap:variable name="General.liquiPropFile" /></code>. Then store your license key in your vault. Specify the path to the license key in <code><MadCap:variable name="General.liquiPropFile" /></code> alongside your other properties:</p>
    </body>
</html>