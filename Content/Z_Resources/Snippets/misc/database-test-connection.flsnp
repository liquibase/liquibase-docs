<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <head>
    </head>
    <body>
        <ol start="4">
            <li>Navigate to your project folder in the&#160;CLI&#160;and run the <MadCap:variable name="General.Liquibase" />&#160;<code>status</code> command to see whether the connection is successful.</li><pre><code class="language-text">liquibase --username=test --password=test --changelog-file=changelog.xml status</code></pre>
            <p class="note" MadCap:autonum="&lt;b&gt;Note: &lt;/b&gt;">Alternatively, you can add the <code>username</code>, <code>password</code>, and <code>changelog-file</code>&#160;<MadCap:variable name="General.Param/Attribute" />s to the <MadCap:variable name="General.Liquibase" /> properties file. Specify the name of the <MadCap:variable name="General.changelog" style="font-style: italic;" /> you created in place of <code>changelog.xml</code>.</p>
            <li>Run your first update with the <MadCap:xref href="../../../commands/update/update.html">update</MadCap:xref> command, which makes changes to your database. You can also run the <MadCap:xref href="../../../commands/update/update-sql.htm">update-sql</MadCap:xref> command to inspect the SQL before running the <code>update</code> command.</li><pre xml:space="preserve"><code class="language-text">liquibase --changelog-file=changelog.xml update-sql
liquibase --changelog-file=changelog.xml update</code></pre>
        </ol>
    </body>
</html>